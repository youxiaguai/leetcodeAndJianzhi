#pragma once
#include "common.h"
vector<vector<int>> queensAttacktheKing(vector<vector<int>>& queens, vector<int>& king) {
	vector<vector<int>>res;
	for (int i = 0; i < queens.size(); i++)
	{
		for (int j= 0; j <queens[0].size(); j++)
		{
			if (queens[i][0]==king[0]||queens[i][1]==king[1]||)
			{
				
			}
		}
	}
}